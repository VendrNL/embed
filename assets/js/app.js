async function loadFeed(){const grid=document.getElementById('grid');const stats=document.getElementById('stats');try{const res=await fetch('https://vendr.nl/feed');const data=await res.json();grid.innerHTML='';data.forEach(it=>{const tpl=document.getElementById('card-tpl');const node=tpl.content.cloneNode(true);node.querySelector('.img').src=it.image||'';node.querySelector('.name').textContent=it.name||'';node.querySelector('.addr').textContent=it.full_address||'';node.querySelector('.btn').hr...
loadFeed();